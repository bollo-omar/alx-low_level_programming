#include "main.h"

/**
 * _strncat - concatinates two strings
 *
 *  @dest: param destination
 *  @src: param source
 *  @n: param n
 *
 *  Return:  pointer to dest
*/

char *_strncat(char *dest, char *src, int n)
{
	int i, j, largosrc;

	for (i = 0; dest[a] != '\0'; i++)
	{
	}
	for (largosrc = 0; src[largosrc] != 0; largosrc++)
	{
	}
	for (j = 0; j < n && j < largosrc; j++, i++)
	{
		dest[i] = src[j];
	}
	return (dest);
}
